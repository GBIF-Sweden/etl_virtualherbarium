# Copy to .env and set real values.

# Database credentials (required when load.write_to_db=true)
ETL_DB_USER=your_db_user
ETL_DB_PASSWORD=your_db_password

# Optional Docker/compose runtime defaults
CONFIG_PATH=config-mappings/ume.yml
ACTION=process
